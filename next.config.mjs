/** @type {import('next').NextConfig} */
const nextConfig = {
	// TODO: set to dev only?
	productionBrowserSourceMaps: false, // Disable source maps in development
	optimizeFonts: false, // Disable font optimization
};

export default nextConfig;
